using UnityEngine;
using System.Collections;

public class EnvironmentRandomize : MonoBehaviour {
	public Vector3[] rockPos,catusPos,fencePos,cratePos;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
